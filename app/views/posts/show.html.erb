<section class="eight columns">

	<div class="post_details">
		<h2><%= @post.title %></h2>
		<%= image_tag @post.image(:header), class: "post_image" %>
		<p><%= @post.body %></p>
	</div>

</section>

<section class="four columns">

  <div>
    <%= render :partial => 'shared/search' %>
  </div>

  <div>
	<%= render :partial => 'shared/postsmenu' %>
  </div>

</section>